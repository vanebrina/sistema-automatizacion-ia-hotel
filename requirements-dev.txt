# Dependencias de desarrollo (tests, lint, formato).
# Instala junto a las de producción:  pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

pytest>=8.0,<9.0
pytest-asyncio>=0.24,<0.25
ruff>=0.7,<0.13
httpx>=0.27,<0.29        # también usado por TestClient de FastAPI
